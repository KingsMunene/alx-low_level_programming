#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point
 * 
 * Return: Always 0 (Success)
 */


int main(void)
{
char genesis = 'a';

while (genesis <= 'z')
{
putchar (genesis);
genesis++;
}
putchar ('\n');
return (0);
}
