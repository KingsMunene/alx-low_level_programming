#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
char start = 'a';

for (a = start; a <= 'z'; a++)
if (a != e || a != q)
putchar (a);

return (0);

}
