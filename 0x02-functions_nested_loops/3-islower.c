#include "main.h"
/**
 * _islower - checks if an alphabet is in lower case
 * @c: is the input being checked
 * Return: returns 0 if not lower and 1 if
 */

int _islower(int c)
{
if (c < 'a')
{
return (0);
}
else
{
return (1);
}
}
