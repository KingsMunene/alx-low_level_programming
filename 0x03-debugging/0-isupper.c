#include "main.h"
/**
 *_isupper - This function checks if a character is uppercase
 *@c: the character being checked
 *Return: Returns 1 if the character is uppercase and zero otherwise
 */

int _isupper(int c)
{
if ( c < 97)
{
return (1);
}
else
{
return (0);
}
}
