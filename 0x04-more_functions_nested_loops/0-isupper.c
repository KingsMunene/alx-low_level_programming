#include "main.h"
/**
 * _isupper - this function checks if a character input is upper case
 * @c: This is the character being checked!
 * Return: returns 1 if is upper and 0 else
 */

int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}
}
