#include "main.h"
/**
 * _isdigit - function to check if a input is a digit
 * @c: The input
 * Return: returns 0 or 1
 */
int _isdigit(int c)
{
if (c >= 0 && c <=10)
{
return (1);
}
else
{
return (0);
}
}
