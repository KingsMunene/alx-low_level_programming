#include "main.h"

/**
 * more_numbers - print 0 to 14 ten times
 * newline 
 */

void more_numbers(void)
{
int c, d;

for (c = 0; c < 10; c++)
{
for (d = 0; d < 14; d++)
{
if ( >= 10)
_putchar(j / 10 + '0');
_putchar(j % 10 + '0');
}
}
_putchar('\n');
}
